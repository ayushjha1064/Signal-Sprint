import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.signalsprint.dustbinmonitor',
  appName: 'DMC Dustbin Monitor',
  webDir: 'dist'
};

export default config;
